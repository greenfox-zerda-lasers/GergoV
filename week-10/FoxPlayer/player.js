// FoxPlayer media player

// Playlist handler

/*

=== DATA INPUT ===
Tracklist - Actual playlist tracks w/ metadata
  - FROM: Playlists module

=== Functions ===
Play/Pause
  Play ends: next track on list
    - if shuffle: random track
    - if last track: first track

Volume

Progress

Shuffle on-off
  - Boolean

*/

var musicPlayer = (function(activePlaylist){

  var activeTracklist = activePlaylist;

  function playTrack() {
    var activeTracklist = activePlaylist;
    // play first track on press play
  }

})();
